# 运行产物与本地环境（不进版本库）
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# 运行日志与输出
logs/
out/

# 用户自己的配置（设备名、语言偏好等因机器而异，不入库）
# 入库的是 config.example.yaml 模板；首次运行会自动复制生成 config.yaml
config.yaml
config.local.yaml

# 编辑器
.vscode/
.idea/
*.swp
